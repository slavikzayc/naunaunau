# naunaunau
для сборки нужен CMake: создать папку для билда, в ней написать cmake .. и cmake --build. .exe файл будет лежать в этой папке.
C++23
для хранения компонентов был выбран unique_ptr, так как он гарантирует, что каждый компонент принадлежит одному конкретному пулу. так же гарантированно корректное удаление этого объекта.
json был выбран из-за своего удобства. либа nlogmann/json была выбрана так как я знаю только ее
